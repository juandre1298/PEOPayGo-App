import { webDataAccessGraphql } from './web-data-access-graphql';

describe('webDataAccessGraphql', () => {
  it('should work', () => {
    expect(webDataAccessGraphql()).toEqual('web-data-access-graphql');
  });
});
